version: '3.8'

services:
  ai-query-prod:
    image: ghcr.io/nervus-tech/nervus-ai-query:v1.1.0-prod
    container_name: ai-query-prod
    ports:
      - "8119:8000"
    environment:
      - DATABASE_URL=postgresql://nervus_user:secure_password@postgres:5432/nervus
      - MINIO_URL=http://minio:9000
      - MINIO_ACCESS_KEY=minio_access
      - MINIO_SECRET_KEY=minio_secret
      - SCHEMA_NAME=prod_ai_query
      - EUREKA_URL=http://eureka:8761
      - SERVICE_PORT=8000
      - SERVICE_NAME=AI-QUERY
      - HOST_NAME=ai-query-prod
    networks:
      - nervus-infrastructure_default

networks:
  nervus-infrastructure_default:
    external: true 