version: '3.8'

services:
  ai-query:
    image: ghcr.io/nervus-tech/nervus-ai-query:prod
    container_name: ai-query-prod
    ports:
      - "8119:8000"
    environment:
      - DATABASE_URL=postgresql://postgres:password@postgres:5432/ai_query_db
    networks:
      - ai-query-network

networks:
  ai-query-network:
    driver: bridge 